name: neutronx
description: A Flutter-first Dart backend framework with shared DTO support and monorepo-native design
version: 0.1.0
homepage: https://github.com/neutronx/neutronx

environment:
  sdk: '>=3.0.0 <4.0.0'

# No external dependencies - pure dart:io implementation
dependencies: {}

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
